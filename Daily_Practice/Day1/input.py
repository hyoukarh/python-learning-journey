# print("Hello " + input("What is your name?") + "!")
# The input function takes a response from the user.
# Then the print function prints out the string and the user input. 

numberOfLetters = len(input())
print(numberOfLetters)
# A variable that stores the number of letters from the user input using the len function. 
# Print then displays the data held inside the variable. 
